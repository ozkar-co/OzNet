<article>
    <header>
        <h1>OzNet - Red Privada</h1>
        <p>Bienvenido a tu red privada basada en ZeroTier</p>
    </header>

    <section>
        <h2>¿Qué es OzNet?</h2>
        <p>
            OzNet es una red privada que funciona sobre la VPN de ZeroTier, proporcionando acceso a múltiples 
            servicios y herramientas en un entorno seguro y controlado. Todos los servicios utilizan el dominio 
            <code>.oznet</code> para facilitar el acceso.
        </p>
    </section>

    <section>
        <h2>Servicios Disponibles</h2>
        <table role="grid">
            <thead>
                <tr>
                    <th>Servicio</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th>Acción</th>
                </tr>
            </thead>
            <tbody>
                {{#each services}}
                <tr>
                    <td><strong>{{name}}</strong></td>
                    <td>{{description}}</td>
                    <td>
                        <span class="status-{{status}}">
                            {{#if (eq status 'active')}}
                                ✅ Activo
                            {{else}}
                                🚧 Próximamente
                            {{/if}}
                        </span>
                    </td>
                    <td>
                        {{#if (eq status 'active')}}
                            <a href="{{url}}" role="button" class="outline">Acceder</a>
                        {{else}}
                            <span class="muted">No disponible</span>
                        {{/if}}
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </section>

    <section>
        <h2>Información de la Red</h2>
        <div class="grid">
            <div>
                <h4>Network ID</h4>
                <code>9bee8941b563441a</code>
            </div>
            <div>
                <h4>Nombre de la Red</h4>
                <p>Oz Network</p>
            </div>
        </div>
    </section>

    <section>
        <h2>Configuración</h2>
        <p>
            Para conectarte a la red OzNet, necesitas configurar ZeroTier, DNS y el certificado SSL. 
            Consulta la <a href="/setup">guía de configuración</a> para obtener instrucciones detalladas.
        </p>
        <div class="grid">
            <div>
                <a href="/setup" role="button">Ver Configuración</a>
            </div>
            <div>
                <a href="/certs/oznet-ca.crt" role="button" class="outline">Descargar Certificado SSL</a>
            </div>
        </div>
    </section>
</article> 